version: "3.9"

services:
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   command: redis-server --appendonly yes
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 5

  agent:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    volumes:
      # Mount projects.json so you can edit it without rebuilding
      - ./projects.json:/projects.json:ro
    # depends_on:
    #   redis:
    #     condition: service_healthy
    restart: unless-stopped

# volumes:
#   redis_data:
